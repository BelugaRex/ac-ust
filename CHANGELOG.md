# Changelog

## 0.8.2

- 修复真实 `:00` 智能周期在页面 timer proof 失败后漏开且诊断假绿：权威页面较晚确认 AC 已 OFF 时，保留原半点所有权，从该次 OFF 证明起满足至少 5 分钟压缩机保护后，以 typed safety retry 补执行仍安全的剩余窗口；不足时才跳到下一半点。恢复、同步、闹钟 admission 与诊断统一拒绝语义无效的智能 ON 时钟，Popup 状态与倒计时改由 `pwmState` 的下一动作推导，不再把下一 OFF 误标成自动开启。
- 加固跨设备同步的失败与交错恢复：timer-only repair 向新旧对端投影明确未来 OFF，已有更早 OFF 不会被延后；慢时钟旧端的显式 disable 可越过全局 watermark。外发 publish 与入站 adopt 共享顺序屏障，失败发布以本机 durable marker 和独立 alarm 重试，入站读取失败单独重试；重叠写完成后丢弃事件副本并重读物理 store，避免一次 storage 故障永久吞掉停用或按错误次序采纳已被覆盖的快照。时钟修复 single-flight 同时绑定 automation revision 与智能边界，不同生命周期会合并为一次尾随修复。
- 自动 ON 改为同页“先保险后开机”事务：初始 OFF 时先在锁定的精确 home tab 写入并确认 `Power-off after`，再进行唯一 ON 点击，最后沿既有独立新鲜页链记录正式 proof；预置失败、URL 漂移和 BFCache 断口均不点击且预置后禁用刷新接力。正常点击仍要求新的 `Execution succeeded` + ON；若返回含糊但同页实际已 ON，则零追加点击，只验证保险。PWM、舒适启动与手动 ON 共用该链，页面原本已 ON 继续零点击直设 timer。
- 按 Fowler 的 Move Function、Introduce Parameter Object 与 Replace Flag Argument 拆分 PWM 生命周期恢复：`smart-recovery.js` 只判断智能当前周期，`interval-recovery.js` 只判断普通循环 alarm/storage，`recovery-coordinator.js` 负责智能优先、循环兜底；启动、看门狗、过期闹钟与诊断时钟统一提交恢复上下文，`background.js` 继续独占开关、`Execution succeeded`、页面定时器、闹钟和 storage 副作用，不新增第二套开机链。
- 修复智能模式在生命周期恢复时把当前理论 ON 窗口快进到下个半点，造成冷气关闭后仍等待十几分钟：启动、过期闹钟和看门狗会在下一 ON 已越过本周期绝对截止时立即补执行剩余窗口，并消费当前半点的新鲜天气建议；关机仍锁定原半点加建议分钟，既有本周期关机闹钟／一分钟失败重试不被抢占，安全余量不足时仍等待下个半点。
- 新增自动控制五分钟舒适启动：仅在本机 `enabled: false→true`，或首次安装采用到已启用配置时执行一次；普通 Popup 打开、浏览器／Service Worker 重启和扩展更新零触发。实际开机前先保留既有更晚页面 timer 并预置至少五分钟保险，正常点击仍等待新的 `Execution succeeded` 与 ON 收敛，已 ON 则零点击；随后按确认 ON 的时刻校准五分钟 floor，经独立新鲜页确认并与 `ac-pwm` 对齐。失败保持安全相位并一分钟后重试，时段外先完成舒适期，用户主动停用仍有最高优先级。Popup 以原位双语状态反馈，不弹启动 alert。
- 修复智能 `:00`/`:30` 闹钟因 Service Worker 唤醒及页面状态读取耗时而整段跳过：`ac-pwm` alarm 与启动期 storage 补执行会把原计划半点传入纯计划器，只在原 ON 绝对截止前且仍能可靠写入 `Power-off after` 时执行剩余相位，不从处理时刻补足时长。页面已经 ON 时显式返回 `alreadyDone`，零开机点击、无需新 `Execution succeeded`，直接进入页面关机定时器链；实际发生点击时的成功提示要求不变。
- 修复智能建议与实际周期不一致：目标边界 plan 丢失时，`:00`/`:30` 执行会改用边界前一小时内的新鲜本地天气按当前灵敏度重算；Service Worker 在 ON 周期中途重启并修复时钟时也会先刷新派生时长并保留原半点锚点，因此界面显示 `21/30` 不再实际沿用旧的 12 分钟。两条路径都不在控制边界联网；过期、边界后或标记 stale 的观测仍拒绝使用。
- 修复不足一分钟的紧急页面定时器目标：例如 `02:11:01` 不再请求会被 UST 归一化的 `02:12`，而是直接写入并验证 `02:13`。诊断同时把已触发但仍在执行的 `ac-pwm` 标为边界处理中，并以实时 AC=OFF 否决陈旧 ON 相位的缺 timer 假警报。
- 收口剩余页面反应时序：观察到本次 `Execution succeeded` 后改为等待目标 ON 状态，状态一旦收敛立即继续且不会盲等 10 秒后重复点击；页面 timer 自动隐藏页只等待 `complete` 的精确 home；picker 每轮输入重试都会重新绑定稳定的唯一语义控件，React 更换 input 后不再继续操作旧节点。所有等待保留原截止／取消检查，持续歧义仍失败关闭。
- 修复页面关机定时器确认后的 React 重绘竞态：写入链会等待 `Power-off after` 恢复为连续稳定的唯一语义控件与目标值，短暂旧／新双树不再误报歧义；AC 状态瞬时不可读时也会短暂等待唯一可读值。持续重复、空值与无法唯一关联的 picker/OK 仍失败关闭。诊断同时识别任意当前相位的一分钟 `ac-pwm` 重试，不再误报独立重试未启用。
- 修复智能控制在 `:00`/`:30` 偶发零点击：自动创建的隐藏控制页与页面定时器新鲜验证页都会等待 `complete` 的精确 home，不再被 `billing-cycle`／warning 等过渡业务子页抢跑；首分钟、`Execution succeeded`、精确 URL 与 OFF 零点击边界不变。诊断同时区分当前 `ac-pwm` 相位重试与独立 `ac-page-timer-retry`，不再把 `pageTimerRetryMinutes=0` 误写成完全无重试。
- 自动开机完成条件收紧为“本次点击后新出现的 `Execution succeeded` + ON 状态”；历史／伪成功提示不采信，等待与确认框轮询并行以免漏掉短暂 toast。提示缺失时不通过刷新或同轮 ON 复核绕过，已是 ON 的幂等预检保持不变。
- 新增先于 Popup 主脚本加载的只读兜底诊断：捕获 `error`/`unhandledrejection`，主诊断未注册时仍可导出脱敏 schedule、`ac-*` alarms、heartbeat、SW 与文档现场并一键复制；各分区独立失败、全程不自愈，也不读取 URL、DOM、账号或余额。完整诊断同时显示本次 Popup 生命周期捕获的运行期异常。
- 补齐 Popup 页面自诊断：报告文档加载/可见状态、视口与内容尺寸、横向溢出、控件与权威 `schedule` 是否同步，以及 Popup→Service Worker 保活连接；设置提交中的瞬态会跳过同步判定，正常纵向滚动不报错，也不采集 URL、DOM 文本或账号信息。
- 重构诊断输出：当前错误、警告与本次修复先汇总，再用稳定错误码、问题域、现场证据和唯一下一步定位首要问题；自动控制关闭、运行时段暂停与智能控制未启用按预期状态展示，不再制造假红灯。后台闹钟自愈返回修复前快照与逐项修复记录，诊断可区分原有闹钟和本次补建；历史异常继续单独展示，不参与当前健康度。
- 加固自动控制竞态与页面适配：本地设置和远端同步共用串行事务，sync busy 时保留最新 mailbox、拒绝陈旧版本并对瞬时读取失败有界重试；页面关机证明只有目标仍在未来才可复用，智能灵敏度请求在 single-flight 或 PWM 占用期间保留尾随重算，且仅在最终设置提交成功后执行。popup 丢弃提交期间的旧轮询与旧响应、同时锁定两个互斥模式，并在运行时段暂停时隐藏余额耗尽估算。页面原生对话框仅在单次 AC 点击栈内代理，隐藏确认框、歧义 AC 开关和无法唯一关联的 `Power-off after` picker/OK 均失败关闭。
- 重构弹窗自动控制信息架构：新增独立总开关统一启停，循环定时与智能控制改为只负责选型的等宽分段单选；关闭时仍可预设模式与参数。运行时段固定使用可访问的 24 小时 `HH:mm` 输入，分钟数非法时原位阻止提交且保留原值；popup 收窄至 280px，中文小号文字按字形视觉补偿 1px。字体改为确定性的平台 UI 栈，macOS 使用 SF／PingFang、Windows 使用 Segoe UI Variable／Microsoft YaHei UI、Linux 优先 Noto CJK，并显式启用 optical sizing；不再因用户是否自行安装 Inter 而改变外观。同步紧凑分段控件、商店截图外框与中英文标签。
- 将「运行时段」从循环定时中抽离为循环定时与智能控制共用的独立自动执行门禁：采用同日半开区间 `[start,end)`，时段外保留 `schedule.enabled` 与所选模式，仅暂停 PWM、角标和看门狗运行闹钟，并通过页面 `Power-off after` 安全停机。重新进入后普通循环可立即恢复，智能控制仍等待下一次 `HH:00`/`HH:30` 首分钟；天气预取与页面关机重试不受暂停影响，自动控制已关闭时单独编辑时段也不会误触发停机。
- 启动、跨设备同步、页面定时器采纳、看门狗、诊断自愈、时钟修复与灵敏度重设统一遵守运行时段门禁；自动 ON 在最终页面消息前、运行闹钟在创建前后再次复检，封堵边界切换期间的异步竞态。退出时段的 runtime 重置到首轮持久化之间会临时封锁旧 storage 回灌；popup 在暂停态保留模式开关、隐藏倒计时、忽略泄漏闹钟的时钟投影，并把运行闹钟修复统一委派给后台，避免诊断绕过最终门禁。暂停或显式重启会取消主世界尚未收敛的自动 ON 递归；安全关机使用独立 revision，恢复后旧验证和旧重试不能覆盖新周期。
- 修复自动 ON 触发页面 BFCache 导航、消息端口关闭时的恢复竞态：旧业务子页即使仍为 `complete` 也不再被误判为控制页已就绪；同一控制标签必须真正回到精确 home 并完成加载后，才进行最多一次幂等开机重试。
- 智能 ON 首分钟截止已过或自动控制 revision 已失效时，后台把结果视为安全终止，不再刷新／导航控制页或误写 `toggle-refresh-recovery` ERROR；真正的消息与页面恢复失败仍保留有限重试和持久诊断。
- 智能控制自动 ON 仅允许在 `HH:00`/`HH:30` 的首分钟启动，错过后等待下一半点；单次 ON 硬上限收紧到 25 分钟，已开启重试即使跨过下一半点、或灵敏度重设后仍沿用原半点绝对截止，超时只安排下一整分钟安全关机。普通 PWM 循环与手动操作继续支持任意分钟，不受该门禁影响。
- 天气调度改为严格未来的 `:20`/`:50` one-shot：提前拉取并生成分别绑定下一 `:30`/`:00` 的本地方案，方案保留原始天气，PWM 控制边界只消费目标完全匹配且足够新鲜的快照，并按当前灵敏度纯本地重算，不直接或间接发起天气网络。预取失败保留最近成功方案，但旧目标方案不会跨边界误用；灵敏度即时重设与 popup 也只读本地天气。
- 智能天气输入改用将军澳 JKB 独立实时气温、相对湿度、10 分钟平均风与站点过去 1 小时雨量，露点由同站温湿度推导；不再混用天文台湿度、静风或西贡区雨量。降雨修正由 `>5 mm` 直接减半改为 $1-0.5\frac{e^{2x}-1}{e^2-1}$（$x=\operatorname{clamp}(r/30,0,1)$）的后段加速曲线，黄雨阈值 30 mm/h 及以上最多减半。
- 智能计算链路保留 `K`、天气观测、`Teq`、降雨修正与 `t_raw` 的浮点精度，只在最终显示和 UST 页面控制边界量化分钟数；popup 与后台共用同一灵敏度兼容归一化函数，诊断新鲜度也改为比较原始毫秒值。
- `Power-off after` 写入方统一生成向上对齐到整分钟的绝对截止时间；页面值、新鲜页证明、正常/恢复 PWM planner、手动开机、时钟修复、智能灵敏度重设、`ac-pwm` 与 popup 倒计时均采用同一个 `targetAt`。
- 诊断报告只显示当前构建以来最新 5 条本机脱敏异常，按时间从新到旧排序；构建边界相等记录保留，构建前、未来与非法时间戳记录忽略，后台环形缓冲仍保留 50 条。
- 构建脚本从同一构建瞬间注入文本时间与数值 epoch，新增对应精度、边界、排序、planner 和 alarm 对齐回归测试。

## 0.8.0

- 新增「智能模式」：按香港天文台天气（气温/湿度推导露点/风速/雨量）自动计算每个 30 分钟周期内的开启时长，覆盖手动时长。
- 严格实现指定算法：灵敏度系数 `K = 0.30 + (档位/10) × 1.00`（档位 0→0.30，10→1.30，共 11 档）；水汽压 `e = 6.112·exp(17.67·Td/(Td+243.5))`；等效室外温度 `Teq = T + 0.33e − 0.70·Wind − 4.00`；30 分钟周期下 `t_raw = K·Teq·(30/60)`；降雨 `> 5 mm` 减半；限幅 `[0, 30]` 取整；压缩机保护（1–4 分钟强制归零）。
- 天气数据源采用香港天文台开放数据 API（`rhrread`）：气温取将军澳站（Tseung Kwan O / JKB，回退西贡/清水湾/天文台）、露点由气温 + 湿度 Magnus 逆推、雨量取西贡区；风速开放数据不提供，按静风保守处理；缓存 1 小时并整点续新，接口不可用时自动退回手动时长。
- 控制周期 30 分钟并锚定半点：ON 相位从半点（HH:00/HH:30）开始，OFF 相位在半点 + 开启分钟数开始，下一 ON 触发对齐到半点。
- 新增 `smart-mode.js` 纯决策模块（无 `chrome.*` 副作用，含 rhrread 解析与露点逆推），`smartMode` 配置随 `chrome.storage.sync` 跨设备同步。
- 智能模式 UI 含总开关、灵敏度滑块（0–10 共 11 档，弱冷←→强冷，实时预览建议分钟数）与等效温度/数据更新时间读数；开启时禁用手动时长输入，关闭时恢复。
- 单元测试增至 336 项，架构校验 143 项全绿。

## 0.7.0

- 将 PWM 阶段推进、过期恢复与 live alarm 对齐提取为可由浏览器和 Node 共用的纯决策模块；Service Worker 只负责执行计划和 Chrome I/O。
- 按 Fowler《重构》纪律对 PWM 主循环、三条停用路径、页面定时器证明记录、启动与闹钟恢复链、纯决策模块长函数、角标去重、快照富化、看门狗恢复分支及弹窗渲染做行为保持型提取重构（29 个 Extract Function）；单元、架构、构建与真实扩展 E2E 验证全部保持全绿。
- 合并 content isolated world 的主世界事件握手实现，并让 PWM 回归直接测试生产 planner，不再依赖旧实现的源码形状。
- 修复余额预计（Est. until）单次页面读取失败即从弹窗消失的问题：控制侧粘住最近一次有效余额，页面重渲染窗口、home 页暂不可读或标签
页被浏览器丢弃时不再让 est 消失，恢复读取后继续更新。

## 0.6.15

- 开机操作收束为带明确次数上限的递归函数；首次未确认时由统一控制页恢复函数刷新当前精确 AC home，或把已偏离的控制标签带回 home，再递归重试一次。
- `content script` 注入失败、bfcache 消息通道关闭、页面明确返回“3 次点击后仍未 ON”等故障统一进入同一恢复链路。
- 每次页面开关点击后继续等待 10 秒再复查；递归恢复次数归零后立即返回，不形成无限刷新或重复发送。

## 0.6.14

- 冷气状态读取、开机和页面定时器读写现在只接受 URL 完整等于 `https://w5.ab.ust.hk/njggt/app/home` 的标签。
- 没有精确 home 时，写操作会创建隐藏 home 标签，不再把 billing-cycle、warning、登录回调或其他 UST 页面导航到 home。
- 新增精确 URL 回归覆盖，拒绝尾斜杠、查询串、哈希、相似路径、业务子页与登录后缀。

## 0.6.13

- 开关冷气前先确认目标标签位于 AC home；若停在 warning 等子页，导航原标签回 home 后再执行。
- 消息端口提前关闭时不再刷新当前页面，改为导航原标签回 AC home、等待内容脚本就绪并仅重试一次。
- 新增最多 50 条的本机脱敏后台异常环形日志，并把最近 10 条并入现有一键复制诊断报告；日志不上传、不跨设备同步，也不记录页面 URL、DOM、余额或账号信息。
- 补充对应回归测试，确保导航恢复不会重复发送开关指令，也不会污染普通错误路径。
