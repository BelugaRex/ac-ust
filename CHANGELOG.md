# Changelog

## 0.8.10

- 调整智能控制保护策略：移除确认关机后的额外五分钟等待，改由每个 `:00`/`:30` 半小时周期最多开启 25 分钟提供保护；自动 ON、重试和恢复统一沿用同一绝对关机截止时间。
- 修复页面卡在 CAS 回调/登录瞬态 URL 时自动开启失效：看门狗每 5 分钟回收长期停留在 `login`/`callback/cas` 且未丢弃的标签，导航回精确 home 以恢复下一轮自动开启的控制页；不打断正常秒级回跳的 CAS 流程。
- 修复页面定时器写入误报「探测超时」：写入超时 30s 短于 content 脚本有界重试最坏时长（约 39s），会提前超时掩盖真实失败阶段；提高到 60s 让真实失败透传。
- 修复 Chrome 后台节流导致定时器「未确认」：写关机时间前短暂置前目标标签（必要时聚焦所在窗口）并写后还原原活动标签，规避后台标签 timer 节流对 AntD picker 确认的影响。
- 修复点击「召唤医生」时 popup 被关掉、且时间已设好仍反复重设：诊断的时钟修复改为只读（不写页面定时器、不重建运行闹钟，仅对齐 storage 时钟），避免诊断触发 setPageTimer 置前标签关闭 popup。
- 修复 Chrome 最小化时仍无法设定关机时间：置前标签补 `state: 'normal'` 取消最小化（仅 `focused: true` 不会把最小化窗口带回前台，窗口仍被判定 occluded、标签继续被节流），并加解除节流等待。
- 修复智能模式提前关机（应开到半点后仍被改成「下一分钟关机」）：时钟修复在 `smartOnBoundaryAt` 陈旧（0/非法）时退回当前半点边界计算关机目标，不再把仍在 ON 窗口内的空调误判为窗口已过。
- 修复定时器「写进去又丢」：开机后新鲜页复核发现定时器被清空（而非未开机）时，现在也补设关机时间，不再只补 1 分钟安全定时器导致提前关机。

## 0.8.9

- 修复智能自动开启偶发「select-ok」失败：上次尝试遗留的时间选择下拉层未关闭，导致本次写入被误判为「多个可见 OK 按钮」而拒绝确认；现写入前先通过 Escape 关闭遗留下拉层。

## 0.8.8

- 修复智能关机边界误设安全定时器：页面关机定时器刚触发时开关可能还没刷新到 OFF，关机边界读到陈旧 ON 会误设 1 分钟页面关机定时器；现读到 ON 时短暂重读确认，确认仍 ON 才补设。

## 0.8.7

- 修复智能自动开启「开了但没设关机时间」：新鲜页验证现在同时读回 AC ON 状态；若复核发现未开机（OFF 态写入的「Power-off after」可能未被服务器保留），先重新开机、确认 ON 后再补设关机时间并再次验证。

## 0.8.6

- 修复智能自动开启偶发「输入框未接受时间（stabilize-control）」：页面重渲染（session 重登录 / AC 状态切换 / billing 刷新）会让「Power-off after」picker 短暂消失或重挂载，原 750ms 稳定等待太短；现延长到 5s，等控件稳定后再写关机时间，避免预布防失败导致整轮自动开启被放弃。

## 0.8.5

- 修复智能模式「不开空调 / 一直开着不关」：自动 ON 预布防失败后，重试改为在原半点周期内重跑「写关机时间 → 确认开机」，不再直接延到下一半点；开机确认由单次读取改为轮询等待页面真正变 ON 后再进入新鲜页验证。
- 修复页面关机定时器写入的两类偶发失败：`Enter` 键在 AC 尚未开机时就触发 onOk 提交、被服务端拒绝并整页刷新，改为直接点选时刻单元格再点 OK 提交；session 重登录 / 状态切换导致「Power-off after」区块短暂消失而误报 `contentNoInput`，改为短等待重定位并加入可恢复失败集强制重注入重试。
- 修复 session 重登录（login → CAS → home）期间恢复逻辑强行导航回 home、打断 CAS 回调造成反复 back/forward cache 断口的问题：现在区分瞬态重登录 URL 与业务漂移 URL，重登录期间只等待自然回跳。
- 诊断透传页面定时器写入的具体失败阶段（type-character / confirm-stable / locate-control 等），便于定位「输入框未接受时间」等问题的真正来源。

## 0.8.4

- 修正 `Power-off after` 的服务器保留时序：该定时器只有在空调已开启（ON）时才会被保留，先输入再立即刷新无效。自动 ON 的预布防改为「本地写入（defer 验证）→ 派发一次开机 → 确认 AC 已 ON → 独立新鲜页读回确认」，避免在 AC 尚未开启时读回为空而被误判失败、导致自动开启永远无法推进；开机后验证失败会补设 1 分钟关机兜底并排 1 分钟重试。
- 将「设置关机时间」收敛为可复用原子 `armPowerOffTimerEnsuringOn`（写 → 确认开机 → 开新页验证），智能模式与 PWM 共用同一时序；PWM 的 ON 相位由三段状态机（set-page-timer / toggle-on / verify-page-timer）收敛为单步原子布防 `arm-page-timer`，开机审计经 `ensureOn` 钩子保留。

## 0.8.3

- 修复自动开启漏执行：自动 ON 现在先通过页面 `Power-off after` 以固定绝对截止时间预布防成功后才允许一次开启派发；单次 React/AntD 输入失效、值回滚或 dropdown 歧义不再让整条开启链终止，重注入与隐藏页兜底都不延长原半点截止，关机侧继续严格 OFF 零点击。
- 修复 Service Worker 异步生命周期竞态：关机写入、restart、相位采纳与主世界递归 ON 现在携带独立 revision 所有权，停用/重启先取消旧请求再允许新周期接管，避免早已过期的关机或开启动作在恢复后继续落地。
- 智能控制天气输入收敛为将军澳 JKB 的气温、相对湿度与风速，停止请求、解析、缓存和使用雨量；旧缓存或预计算方案中的多余字段继续兼容忽略，不影响建议分钟数、控制理由或半点边界行为。
- 新增仅打包构建启用的本机控制生命周期审计：「召唤医生」开始前先快照当前构建事故链，逾期未确认的自动 ON 显示「自动开启未确认」，不再被普通倒计时掩盖。
- 将弹窗的「运行诊断」更名为「召唤医生 / Call the doctor」，同步中英文进度、结果与复制反馈；医生会检查状态并尝试自动修复常见问题，但不保证一定成功。
- 将弹窗的「运行时段」更名为「限时运行 / Limit operating hours」，并明确提示关闭限制即全天运行，避免用户误以为持续自动控制必须先配置时段。
- 医生检查报告分别展示智能自动 ON 的 `ac-pwm` 页面安全定时器重试与独立 OFF 的 `ac-page-timer-retry`；typed marker、storage 和 live alarm 缺失或错位会明确判红，`pageTimerRetryMinutes` 只表示 OFF 通道写入页面的目标关机分钟数。

## 0.8.2

- 将「运行时段」从循环定时中抽离为循环定时与智能控制共用的独立自动执行门禁：采用同日半开区间 `[start,end)`，时段外保留 `schedule.enabled` 与所选模式，仅暂停 PWM、角标和看门狗运行闹钟，并通过页面 `Power-off after` 安全停机。重新进入后普通循环可立即恢复，智能控制仍等待下一次 `HH:00`/`HH:30` 首分钟；天气预取与页面关机重试不受暂停影响，自动控制已关闭时单独编辑时段也不会误触发停机。
- 启动、跨设备同步、页面定时器采纳、看门狗、诊断自愈、时钟修复与灵敏度重设统一遵守运行时段门禁；自动 ON 在最终页面消息前、运行闹钟在创建前后再次复检，封堵边界切换期间的异步竞态。退出时段的 runtime 重置到首轮持久化之间会临时封锁旧 storage 回灌；popup 在暂停态保留模式开关、隐藏倒计时、忽略泄漏闹钟的时钟投影，并把运行闹钟修复统一委派给后台，避免诊断绕过最终门禁。
- 修复自动 ON 触发页面 BFCache 导航、消息端口关闭时的恢复竞态：旧业务子页即使仍为 `complete` 也不再被误判为控制页已就绪；同一控制标签必须真正回到精确 home 并完成加载后，才进行最多一次幂等开机重试。
- 智能控制自动 ON 仅允许在 `HH:00`/`HH:30` 的首分钟启动，错过后等待下一半点；单次 ON 硬上限收紧到 25 分钟，已开启重试即使跨过下一半点、或灵敏度重设后仍沿用原半点绝对截止，超时只安排下一整分钟安全关机。普通 PWM 循环与手动操作继续支持任意分钟，不受该门禁影响。
- 天气调度改为严格未来的 `:10`/`:50` one-shot：提前拉取并生成分别绑定下一 `:30`/`:00` 的本地方案，方案保留原始天气，PWM 控制边界只消费目标完全匹配且足够新鲜的快照，并按当前灵敏度纯本地重算，不直接或间接发起天气网络。预取失败保留最近成功方案，但旧目标方案不会跨边界误用；灵敏度即时重设与 popup 也只读本地天气。
- 智能天气输入改用将军澳 JKB 独立实时气温、相对湿度、10 分钟平均风与站点过去 1 小时雨量，露点由同站温湿度推导；不再混用天文台湿度、静风或西贡区雨量。降雨修正由 `>5 mm` 直接减半改为 $1-0.5\frac{e^{2x}-1}{e^2-1}$（$x=\operatorname{clamp}(r/30,0,1)$）的后段加速曲线，黄雨阈值 30 mm/h 及以上最多减半。
- 智能计算链路保留 `K`、天气观测、`Teq`、降雨修正与 `t_raw` 的浮点精度，只在最终显示和 UST 页面控制边界量化分钟数；popup 与后台共用同一灵敏度兼容归一化函数，诊断新鲜度也改为比较原始毫秒值。
- `Power-off after` 写入方统一生成向上对齐到整分钟的绝对截止时间；页面值、新鲜页证明、正常/恢复 PWM planner、手动开机、时钟修复、智能灵敏度重设、`ac-pwm` 与 popup 倒计时均采用同一个 `targetAt`。
- 诊断报告只显示当前构建以来最新 5 条本机脱敏异常，按时间从新到旧排序；构建边界相等记录保留，构建前、未来与非法时间戳记录忽略，后台环形缓冲仍保留 50 条。
- 构建脚本从同一构建瞬间注入文本时间与数值 epoch，新增对应精度、边界、排序、planner 和 alarm 对齐回归测试。

## 0.8.0

- 新增「智能模式」：按香港天文台天气（气温/湿度推导露点/风速/雨量）自动计算每个 30 分钟周期内的开启时长，覆盖手动时长。
- 严格实现指定算法：灵敏度系数 `K = 0.30 + (档位/10) × 1.00`（档位 0→0.30，10→1.30，共 11 档）；水汽压 `e = 6.112·exp(17.67·Td/(Td+243.5))`；等效室外温度 `Teq = T + 0.33e − 0.70·Wind − 4.00`；30 分钟周期下 `t_raw = K·Teq·(30/60)`；降雨 `> 5 mm` 减半；限幅 `[0, 30]` 取整；压缩机保护（1–4 分钟强制归零）。
- 天气数据源采用香港天文台开放数据 API（`rhrread`）：气温取将军澳站（Tseung Kwan O / JKB，回退西贡/清水湾/天文台）、露点由气温 + 湿度 Magnus 逆推、雨量取西贡区；风速开放数据不提供，按静风保守处理；缓存 1 小时并整点续新，接口不可用时自动退回手动时长。
- 控制周期 30 分钟并锚定半点：ON 相位从半点（HH:00/HH:30）开始，OFF 相位在半点 + 开启分钟数开始，下一 ON 触发对齐到半点。
- 新增 `smart-mode.js` 纯决策模块（无 `chrome.*` 副作用，含 rhrread 解析与露点逆推），`smartMode` 配置随 `chrome.storage.sync` 跨设备同步。
- 智能模式 UI 含总开关、灵敏度滑块（0–10 共 11 档，弱冷←→强冷，实时预览建议分钟数）与等效温度/数据更新时间读数；开启时禁用手动时长输入，关闭时恢复。
- 单元测试增至 336 项，架构校验 143 项全绿。

## 0.7.0

- 将 PWM 阶段推进、过期恢复与 live alarm 对齐提取为可由浏览器和 Node 共用的纯决策模块；Service Worker 只负责执行计划和 Chrome I/O。
- 按 Fowler《重构》纪律对 PWM 主循环、三条停用路径、页面定时器证明记录、启动与闹钟恢复链、纯决策模块长函数、角标去重、快照富化、看门狗恢复分支及弹窗渲染做行为保持型提取重构（29 个 Extract Function）；单元、架构、构建与真实扩展 E2E 验证全部保持全绿。
- 合并 content isolated world 的主世界事件握手实现，并让 PWM 回归直接测试生产 planner，不再依赖旧实现的源码形状。
- 修复余额预计（Est. until）单次页面读取失败即从弹窗消失的问题：控制侧粘住最近一次有效余额，页面重渲染窗口、home 页暂不可读或标签
页被浏览器丢弃时不再让 est 消失，恢复读取后继续更新。

## 0.6.15

- 开机操作收束为带明确次数上限的递归函数；首次未确认时由统一控制页恢复函数刷新当前精确 AC home，或把已偏离的控制标签带回 home，再递归重试一次。
- `content script` 注入失败、bfcache 消息通道关闭、页面明确返回“3 次点击后仍未 ON”等故障统一进入同一恢复链路。
- 每次页面开关点击后继续等待 10 秒再复查；递归恢复次数归零后立即返回，不形成无限刷新或重复发送。

## 0.6.14

- 冷气状态读取、开机和页面定时器读写现在只接受 URL 完整等于 `https://w5.ab.ust.hk/njggt/app/home` 的标签。
- 没有精确 home 时，写操作会创建隐藏 home 标签，不再把 billing-cycle、warning、登录回调或其他 UST 页面导航到 home。
- 新增精确 URL 回归覆盖，拒绝尾斜杠、查询串、哈希、相似路径、业务子页与登录后缀。

## 0.6.13

- 开关冷气前先确认目标标签位于 AC home；若停在 warning 等子页，导航原标签回 home 后再执行。
- 消息端口提前关闭时不再刷新当前页面，改为导航原标签回 AC home、等待内容脚本就绪并仅重试一次。
- 新增最多 50 条的本机脱敏后台异常环形日志，并把最近 10 条并入现有一键复制诊断报告；日志不上传、不跨设备同步，也不记录页面 URL、DOM、余额或账号信息。
- 补充对应回归测试，确保导航恢复不会重复发送开关指令，也不会污染普通错误路径。